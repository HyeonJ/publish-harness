# Publishing Log

Source:
- Figma URL:
- fileKey:
- Agent:
- Harness commit:

## Route Discovery

Record route/page discovery before implementation. If the Figma URL has no
`node-id`, run:

```bash
node scripts/discover-figma-pages.mjs --file-key <fileKey> --out docs/figma-pages.md --apply
```

| Page | Route | Desktop Node | Tablet Node | Mobile Node | Status |
|---|---|---|---|---|---|
| Home | `/` | | | | |

## Reuse Plan

List shared components before implementing sections.

| Component | Path | Reused by | Source |
|---|---|---|---|
| SiteLayout | `src/components/layout/SiteLayout.tsx` | all pages | Phase 2 |
| Header | `src/components/layout/Header.tsx` | all pages | Phase 2 |
| Footer | `src/components/layout/Footer.tsx` | all pages | Phase 2 |

## Timeline

| Step | Time spent | Status | Notes |
|---|---:|---|---|
| Bootstrap | | | |
| Adopt existing project | | | Use only when the target directory was not empty. |
| Route discovery | | | |
| Reuse plan | | | |
| Implementation | | | |
| Gates | | | |

## Gate Results

Record every executed command, not only the summary. If only one route was
checked, say so.

| Gate | Command | Status | Result file | Notes |
|---|---|---|---|---|
| Structure | | | | |
| Lint | | | | |
| Typecheck | | | | |
| Build | | | | |
| Quality `<page-or-route>` | `npm run quality -- <page> <dir>` | | | Add one row per discovered route/page. |
| Final verifier | `node scripts/verify-publishing-complete.mjs` | | | Required before reporting completion. |

## Anchor Coverage

For figma mode, copy the G1 L2 coverage from `tests/quality/<page>.json`.
`requiredMatched` must equal `requiredTotal` before completion.

| Page/section | Viewport | anchorsMatched / anchorsTotal | requiredMatched / requiredTotal | G1 categories | Notes |
|---|---|---:|---:|---|---|
| Home | desktop | | | | |

## Visual Drift Follow-Up

When G1 fails, separate L1 pixel diff from L2 anchor drift. Fix in this order:
required anchors, decorative flow/layering, wrong anchor target, repeated image
stack drift, section height explosion, typography/text metrics, then remaining
pixel diff.

| Page/section | L1 diff % | First L2 failure | Category | Fix plan | Result |
|---|---:|---|---|---|---|
| | | | | | |

## Progress State

After implementation, `progress.json` and `PROGRESS.md` must match the actual
project state. Record any mismatch here.

| Item | Expected | Actual | Follow-up |
|---|---|---|---|
| Phase | | | |
| Pages | | | |
| Sections | | | |

## Errors And Issues

Record issues with their root cause, not only the workaround.

If bootstrap was skipped, record it as a workflow deviation and list the manual
adoption artifacts that were created: `progress.json`, `PROGRESS.md`, `scripts`,
`docs/project-context.md`, `docs/reusable-react-publishing.md`, and
`docs/figma-pages.md`.

| Time | Issue | Root cause | Resolution | Follow-up |
|---|---|---|---|---|
| | | | | |
